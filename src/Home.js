import React from 'react';

const Home = () => (
  <section className="hero">
    <div className="container">
      <h2>Home</h2>
      <p>This is the home section with a hero image or gradient background.</p>
    </div>
  </section>
);

export default Home;
